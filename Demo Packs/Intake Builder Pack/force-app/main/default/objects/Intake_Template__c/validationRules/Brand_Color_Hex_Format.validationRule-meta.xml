<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Brand_Color_Hex_Format</fullName>
    <active>true</active>
    <description>Brand_Color__c must be a 6-digit hex color prefixed with # (e.g. #032D60).</description>
    <errorConditionFormula>AND(
    NOT(ISBLANK(Brand_Color__c)),
    NOT(REGEX(Brand_Color__c, &quot;^#[0-9A-Fa-f]{6}$&quot;))
)</errorConditionFormula>
    <errorMessage>Brand Color must be a 6-digit hex color prefixed with # (e.g. #032D60).</errorMessage>
</ValidationRule>
